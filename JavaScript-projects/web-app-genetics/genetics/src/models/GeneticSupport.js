class GeneticSupport {

    constructor(name, nature, alleles, rules) {
        this.name = name,
            this.nature = nature,
            this.alleles = alleles,
            this.rules = rules
    }
}

export default GeneticSupport